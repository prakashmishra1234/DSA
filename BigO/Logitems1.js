// O(N) -- Number of operations is proportional to N

function logItems(n) {
  for (let i = 0; i < n; i++) {
    console.log(i);
  }
}

logItems(10);

//In the above example number of iteration is equal to n which is 10;
